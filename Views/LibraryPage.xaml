<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LTFLauncher.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LTFLauncher.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ItemsControl ItemsSource="{x:Bind CardItems}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="10" CornerRadius="10">
                        <Border.Background>
                            <AcrylicBrush TintColor="#FF000000" TintOpacity="0.6" FallbackColor="#FF000000"/>
                        </Border.Background>
                        <StackPanel>
                            <Image Source="{Binding ImageSource}" Height="100" Width="100"/>
                            <TextBlock Text="{Binding Text}" Margin="5"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
